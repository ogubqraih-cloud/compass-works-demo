# Claude API キー（サーバー側のみで使用。フロントには絶対に出さない）
# 取得: https://console.anthropic.com/  →  sk-ant- で始まる文字列
# ローカル: このファイルを参考に export ANTHROPIC_API_KEY=... する（.env はコミットしない）
# 本番(Vercel): ダッシュボード → Settings → Environment Variables に登録
ANTHROPIC_API_KEY=
